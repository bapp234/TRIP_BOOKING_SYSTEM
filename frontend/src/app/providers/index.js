import React from 'react'

// App-level providers should be composed here. Minimal passthrough for now.
export const AppProviders = ({ children }) => {
  return <>{children}</>
}

export default AppProviders
