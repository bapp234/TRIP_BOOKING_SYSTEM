/**
 * App setup skeleton
 *
 * Exports a `setupApp` function which accepts an existing Express-style
 * `app` instance and wires middleware, routes, and error handlers.
 *
 * NOTE: This file contains skeleton wiring only—no business, DB, or auth logic.
 */

module.exports = {
  setupApp: (app) => {
    // Mount global middlewares here (body parser, cors, etc.)
    // Mount routes: e.g. app.use('/api', require('./routes'))

    // Mount error handlers here
  },
};
